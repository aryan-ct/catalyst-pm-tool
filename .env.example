# API
PORT=3000
JWT_SECRET=your-secret-here

# Database (used by Prisma + docker compose)
DATABASE_URL=postgresql://postgres:postgres@db:5432/catalyst_pm

# Postgres container (docker compose only)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=catalyst_pm
