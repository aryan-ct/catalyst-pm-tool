model Resource {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String
  password  String
  role      Role
  isActive  Boolean
  leads     Lead[]
  tasks     Task[]   @relation("TaskToResource")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum Role {
  MANAGER
  DEV
  TESTER
  DESIGNER
  HR
  JR_HR
  BDE
}
